{
  "name": "@pulse/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pulse monitoring engine — runs site checks on GitHub Actions and writes JSON results into the repo.",
  "scripts": {
    "monitor": "tsx src/index.ts",
    "monitor:dry": "tsx src/index.ts --dry-run",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pulse/shared": "*",
    "yaml": "^2.6.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.2.6"
  }
}
